use crate::modules::persistent_storage::SharedStorageReference;
use super::SharedMetadataBackupPtr;


pub(crate) fn persist(dirty_list: *mut u8, backup_list: SharedMetadataBackupPtr, storage_ref: SharedStorageReference) {
    
}
